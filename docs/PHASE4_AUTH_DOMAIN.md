# Phase 4 — Auth & Domain Isolation (Complete)\n\n## Auth Roles (Supabase)\n- Migrations: `0012_auth_role_sync.sql` (handle_new_user, sync_profile_role_to_auth)\n- Roles: `customer` (default), `admin`, `super_admin`\n- Source of truth: `auth.users.raw_app_meta_data` + `public.profiles.role`\n- Client validation: `src/utils/roleUtils.ts` (extractRoleFromAppMetadata, hasAdminRole, canAccessAdminDomain)\n- Admin access requires `isAdmin` (`admin` or `super_admin`)\n\n## Domain Isolation\n- `DomainGuard` (`src/routes/DomainGuard.tsx`): blocks `/admin` on customer domain (`kixora.com`) with 404\n- `AdminRoute` (`src/routes/AdminRoute.tsx`): requires `isAdmin`; shows 403 for customer session\n- `Navbar`: no public \"Admin Portal\" link; admin button only when `isAdminDomain` true\n- `inspectHostname`: uses `VITE_ADMIN_ORIGIN` / `adminDomain` config; allows `admin.localhost` for local dev\n\n## Env / CORS (local + prod)\n- `.env` variables:\n  - `CUSTOMER_ORIGIN=https://kixora.com`\n  - `ADMIN_ORIGIN=https://admin.kixora.com`\n  - `CORS_ALLOWED_ORIGINS=https://kixora.com,https://admin.kixora.com`\n  - `VITE_CUSTOMER_ORIGIN=https://kixora.com`\n  - `VITE_ADMIN_ORIGIN=https://admin.kixora.com`\n- Local dev: use `admin.localhost` (http) or set `VITE_PLAYWRIGHT_ADMIN=true` + `?domain=admin`\n- Production: both origins must be HTTPS and listed in `CORS_ALLOWED_ORIGINS`; never `*`\n\n## Code Splitting\n- `AdminDashboard` loaded via `React.lazy()` in `src/App.tsx` (line 16)\n- Wrapped in `<React.Suspense>` inside admin view only\n- Admin bundle not required on storefront path\n\n## How to open admin safely in dev\n1. Run storefront on `localhost:5173` (customer domain)\n2. Open admin on `http://admin.localhost:5173` (or use `?domain=admin` with `VITE_PLAYWRIGHT_ADMIN=true`)\n3. Sign in with admin/super_admin credentials; customer session gets 403\n4. Do NOT expose `/admin` link on public navbar\n\n## Build Status\n- No payments / Cloudinary / PWA / Phase 5 3D changes made\n- Admin bundle split; auth roles enforced; domain guard active\n- Ready for build verification\n