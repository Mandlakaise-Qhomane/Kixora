node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
playwright-report/
test-results/
lint_results.json
test_output.txt
playwright_output.txt

# --- Guard: stray artifacts from mistyped terminal / agent commands ---
# A botched remote-heads command once created a file literally named
# "-remote --heads fork develop", and a shell/pager mishap created files named
# after SQL fragments (e.g. "elect p.proname,"). Never let these reach the repo.
/-*
lint_output.txt
migration-drift-diff.txt
